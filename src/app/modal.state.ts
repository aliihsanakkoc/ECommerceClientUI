import { signal } from "@angular/core";

export const showLoginModalSignal = signal(false);
export const showCartModalSignal = signal(false);
export const showAccountModalSignal = signal(false);
export const showContactModalSignal = signal(false);
export const showCategoryModalSignal = signal(false);
